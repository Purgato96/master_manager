name: Deploy to Oracle Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Executando o Deploy no Servidor
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /home/ubuntu/master_manager
            git pull origin main
            docker compose -f docker-compose.yml up -d --build
            docker compose -f docker-compose.yml exec -T backend php artisan migrate --force
            docker compose -f docker-compose.yml exec -T backend php artisan optimize
            docker compose -f docker-compose.yml restart queue scheduler